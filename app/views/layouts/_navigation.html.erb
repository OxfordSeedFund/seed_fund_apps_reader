<nav class="navbar-default navbar-static-side" role="navigation">
    <div class="sidebar-collapse">
        <ul class="nav metismenu" id="side-menu">
            <li class="nav-header">
                <div class="dropdown profile-element">
                    <a data-toggle="dropdown" class="dropdown-toggle" href="#">
                        <span class="clear">
                            <span class="block m-t-xs">
                                <strong class="font-bold"><%= image_tag("logo.png", width:'100%', class: 'img-responsive') %></strong>
                            </span>
                        </span>
                    </a>
                </div>
                <!-- <a class="navbar-minimalize" href="#"></a> -->
                <div class="logo-element">
                    HH
                </div>
            </li>
            <li class="<%= 'active' if current_page?('/') %>">
                <%= link_to "/" do %>
                <i class="fa fa-file"></i> <span class="nav-label">Full App List</span>
                <% end %>
            </li>
            <% for member in @members %>
                <li class="<%= 'active' if request.env['PATH_INFO'].include?("/members/"+member.id.to_s) %>">
                    <%= link_to member_path(member) do %>
                        <i class="fa fa-file"></i> <span class="nav-label"><%= member.name %></span>
                        <span class="fa arrow"></span>
                    <% end %>
                    <ul class="nav nav-second-level">
                        <% for app in member.apps %>
                            <li class="<%= 'active' if current_page?(member_app_path(member.id, app.id)) %>">
                                <%= link_to member_app_path(member.id, app.id) do %><i class="fa fa-file-text"></i> Application <%= app.id %><% end %>
                            </li>
                        <% end %>
                    </ul>
                </li>
            <% end %>
        </ul>
    </div>
</nav>
<script type="text/javascript">
$('li').hasClass("active").parent()
})

</script>
